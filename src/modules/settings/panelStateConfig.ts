import type { PanelStateConfig } from "@/types/panels";
import { AppPanels } from "..";

export const settingState : PanelStateConfig = {
  [AppPanels.settings]: {
    fields: {},
    actions: {}
  },
};