import type { PanelStateConfig } from '@/types/panels'
import { AppPanels } from '..';

export const searchResultState : PanelStateConfig = {
  [AppPanels.searchResults]: {
    fields: {},
    actions: {}
  },
};